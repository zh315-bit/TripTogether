# Application and local ASGI server (no optional extras).
fastapi>=0.115,<1.0
pydantic>=2.9,<3.0
uvicorn>=0.30,<1.0

# PostgreSQL infrastructure and local environment configuration.
SQLAlchemy>=2.0,<2.1
psycopg[binary]>=3.2,<4.0
python-dotenv>=1.0,<2.0
alembic>=1.13,<2.0

# Registration validation and password hashing.
email-validator>=2.2,<3.0
argon2-cffi>=23.1,<26.0
PyJWT>=2.10.1,<3.0

# Regression and contract tests; TestClient transport.
pytest>=8.0,<9.0
httpx>=0.27,<1.0
